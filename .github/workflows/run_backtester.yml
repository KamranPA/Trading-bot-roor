name: Run Backtester and Optimizer

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgomp1

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests ccxt numpy joblib yfinance lightgbm psycopg2-binary

      # ۱. دریافت داده‌ها
      - name: Fetch Market Historical Data
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python fetcher.py

      # ۲. اجرای بک‌تستر
      - name: Run Backtest
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python src/backtester.py

      # ۳. بهینه‌سازی پارامترها
      - name: Run Optimizer
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python src/optimizer.py

      # ۴. آموزش مدل
      - name: Train AI Model
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python src/train_model.py

      # ۵. کامیت و پوش هوشمند (اصلاح‌شده برای رفع خطای تداخلات گیت)
      - name: Commit and Push Results
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          
          # پاکسازی محیط کاری از فایل‌های مزاحم
          git reset --hard
          
          # اضافه کردن فایل‌های خروجی به استیج
          git add best_params.json backtest_table_summary.csv trading_bot_backtest.db models/ 2>/dev/null || echo "Some files not found."
          git add data/4h/*.csv 2>/dev/null || echo "No CSV files in data/4h."
          
          # ثبت تغییرات و پوش به ریپازیتوری
          if git diff --cached --quiet; then
            echo "No changes detected."
          else
            git commit -m "Auto-update: Saving backtest results and AI models"
            git push origin main --force
          fi

      # ۶. آپلود خروجی به صورت Artifact برای دانلود مستقیم
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backtest-results
          path: |
            backtest_table_summary.csv
            best_params.json
